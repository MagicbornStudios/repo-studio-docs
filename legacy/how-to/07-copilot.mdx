---
title: 07 - Copilot and AI integration

created: 2026-02-04
updated: 2026-02-07
---

# 07 - Copilot and AI integration

Context, actions, suggestions, highlights; plan-execute-review-commit; app-level actions. Single source of truth: [architecture/03-copilotkit-and-agents.mdx](../architecture/03-copilotkit-and-agents.mdx).

## Context (what the agent sees)

- **Shell**: `useCopilotReadable` in AppShell -- activeWorkspaceId, openWorkspaceIds, editor labels, editor summary.
- **System**: When an editor is active, `useDomainCopilot(contract)` registers `contract.getContextSnapshot()` as readable (domain/system id, editorId, selection, selectionSummary, domainState). System instructions come from `contract.getInstructions()`.

## Actions (what the agent can do)

- **Shell**: `switchEditor`, `openEditor`, `closeEditor` (no prefix).
- **System**: All actions from `contract.createActions()` with system prefix (e.g. `forge_createNode`, `forge_executePlan`, `forge_commit`). Each registered via `useCopilotAction`.
- **App-level**: Registered in AppShell: `app_generateImage`, `app_respondWithStructure`. Use a neutral prefix so they don't collide with system actions.

## Suggestions and highlights

- **Suggestions**: `contract.getSuggestions()` -> chips in chat.
- **Highlights**: After an action, handlers can call `onAIHighlight({ entities: { 'forge.node': [id] } })`; the UI highlights those entities briefly. Same `AIHighlightPayload` and `useAIHighlight` across systems.

## Plan-execute-review-commit

Dialogue Editor (and any editor that adopts it): create plan (no draft change) -> plan renders inline in chat via **PlanCard** with Apply/Dismiss -> execute plan (apply steps, set pending) -> **WorkspaceReviewBar** (Revert / Accept) -> commit (save). See Section 11 in [architecture/03-copilotkit-and-agents.mdx](../architecture/03-copilotkit-and-agents.mdx).

## What the AI can do (summary)

At full integration: read editor and system context; run shell and system actions; for Dialogue, create/execute plan and commit after review; use app actions for image generation and structured output. Co-agents are documented but not used yet; see [17-co-agents-and-multi-agent.mdx](../17-co-agents-and-multi-agent.mdx).

See also: [08 - Adding AI to editors](08-adding-ai-to-workspaces.md).

**Next:** Back to [00 - Index](00-index.md) or reference docs (decisions, tech-stack, STATUS).
